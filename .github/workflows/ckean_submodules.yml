name: Reset submodules

on: push

permissions:
  packages: write
  contents: write


jobs:
  variables:
    runs-on: ubuntu-latest
    steps:
      - run: | 
          git config --set submodule.python.active False || 
          git config --unset submodule.python.active && 
          git config --add submodule.python.active False
